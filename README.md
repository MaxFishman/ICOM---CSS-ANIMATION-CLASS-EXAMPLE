# ICOM---CSS-ANIMATION-CLASS-EXAMPLE

Interactive HTML/CSS learning page that demonstrates CSS transitions and animations.

## Run locally
Open `index.html` in a browser.

## GitHub Pages
This repository is ready for GitHub Pages hosting from the root branch because `index.html` is at the project root.
